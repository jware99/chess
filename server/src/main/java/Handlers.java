import spark.Route;

public class Handlers implements Route { //convert http to java and back to json

    //loginHandler, registerHandler, logoutHandler, joingameHandler, creategameHandler, listGamesHandler, clearHandler
}
